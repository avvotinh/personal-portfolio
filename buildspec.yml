version: 0.2

run-as: Ubuntu

phases:
  install:
    run-as: Ubuntu
    runtime-versions:
      nodejs: 10
  build:
    run-as: Ubuntu
    commands:
      - echo Nothing to do yet
artifacts:
  file:
    - index.html
    - favicon.co
    - Software-Engineer-Hop-Le.pdf
    - styles/main.css
    - images/*
